{
  "name": "pen-to-figma-importer",
  "version": "1.0.0",
  "description": "Figma plugin to import .pen design files as fully editable Figma documents",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.98.0",
    "esbuild": "^0.20.0",
    "typescript": "^5.3.0"
  }
}
